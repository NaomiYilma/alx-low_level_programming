#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 * main - Entry point
 *
 * Description: 'using putchar'
 *
 * Return: 'Always return zero'
 */
int main(void)
{
	char ch = {a..z};

	for (ch = 'a'; ch <= 'z'; ch++)
		putchar(ch);

        return (0);
}
