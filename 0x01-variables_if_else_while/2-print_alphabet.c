#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 * main - Entry point
 *
 * Description: 'using putchar'
 *
 * Return: 'Always return zero'
 */
int main(void)
{
	char alphabet[26] = "abcdefghijklmnopqrstuvwxyz";

	for (ch = 0; ch <= 26; ch++)
	{
		putchar(alphabet[ch]);
	}
	putchar {'\n'};
        return (0);
}
