#include <stdio.h>
/**
 * main - all single digit numbers of base 10
 *
 * Return: Always returns 0
 */
int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
		printf("%d", i)
	printf('\n');
	return (0);
}
