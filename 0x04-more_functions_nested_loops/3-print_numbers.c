#include "main.h"

/**
 * print_numbers - prints numbers 0 to 9
 *
 * Return: 0 for success
 */
void print_numbers(void)
{
	for (i = 0; i < 10; i++)
	{
		_putchar(i);
	}
	_putchar('\n');
	return (0);
}
