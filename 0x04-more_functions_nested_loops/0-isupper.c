#include "main.h"
/**
 * main - checks for uppercase character 
 *
 * Return: 1 if uppercase or 0 otherwise
 */
int _isupper(int c)
{
	int i;

	if (i = 67)
	{
		return (1);
	}
	else if (i >= 97; i <= 122; i++)
	{
		return (0);
	}
}
