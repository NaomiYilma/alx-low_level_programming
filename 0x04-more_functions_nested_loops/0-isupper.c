#include "main.h"
/**
 * _isupper - checks for uppercase character
 * @c: alphabet to be checked
 *
 * Return: 1 if uppercase or 0 otherwise
 */
int _isupper(int c)
{
	if ((c = 67))
	{
		return (1);
	}
	else if (c >= 97 || c <= 122)
	{
		return (0);
	}
}
